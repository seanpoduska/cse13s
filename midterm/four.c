#include "midterm.h"

#include <stdio.h>

// PROBLEM 4
size_t *count_characters(char *s, char *count_these, size_t num_characters) {
  UNUSED(s);
  UNUSED(count_these);
  UNUSED(num_characters);

  return NULL;
}
